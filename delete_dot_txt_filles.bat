@echo off
del /s /q *.txt
echo All .txt files have been deleted.

rem @echo off: Disables the echoing of commands in the console.
rem del: Deletes files.
rem /s: Deletes files in the current directory and all subdirectories.
rem /q: Quiet mode, which suppresses confirmation prompts.
rem *.txt: Specifies all files with the .txt extension.
